diff --git a/fcitx5/addon/vocotype.cpp b/fcitx5/addon/vocotype.cpp
index 0dcb7f2..cafc0f0 100644
--- a/vocotype.cpp
+++ b/vocotype.cpp
@@ -66,8 +66,7 @@ VoCoTypeAddon::VoCoTypeAddon(fcitx::Instance* instance)
     // 获取安装路径
     const char* home = std::getenv("HOME");
     if (home) {
-        python_venv_path_ = std::string(home) + "/.local/share/vocotype-fcitx5/.venv/bin/python";
-        recorder_script_path_ = std::string(home) + "/.local/share/vocotype-fcitx5/backend/audio_recorder.py";
+        recorder_script_path_ = "/usr/bin/vocotype-audio-recorder";
     } else {
         FCITX_ERROR() << "HOME environment variable not set";
     }
@@ -215,7 +214,7 @@ void VoCoTypeAddon::startRecording(fcitx::InputContext* ic) {
         return;
     }
 
-    if (python_venv_path_.empty() || recorder_script_path_.empty()) {
+    if (recorder_script_path_.empty()) {
         showError(ic, "录音配置无效");
         return;
     }
@@ -252,9 +251,7 @@ void VoCoTypeAddon::startRecording(fcitx::InputContext* ic) {
         close(stdout_pipe[0]);
         close(stdout_pipe[1]);
 
-        execl(python_venv_path_.c_str(),
-              python_venv_path_.c_str(),
-              recorder_script_path_.c_str(),
+        execl(recorder_script_path_.c_str(),
               static_cast<char*>(nullptr));
         _exit(127);
     }
